document.addEventListener('DOMContentLoaded',()=>{const cards=document.querySelectorAll('.guide-card-alt');const modal=document.getElementById('video-modal');const closeBtn=modal.querySelector('.video-modal-close');const playerFrame=document.getElementById('youtube-player');const baseYouTubeUrl="https://www.youtube.com/embed/";cards.forEach(card=>{card.addEventListener('click',()=>{const videoId=card.getAttribute('data-video-url');if(videoId){const videoUrl=`${baseYouTubeUrl}${videoId}?autoplay=1&rel=0&modestbranding=1`;playerFrame.src=videoUrl;modal.classList.add('open');document.body.style.overflow='hidden'}})});const closeModal=()=>{playerFrame.src="";modal.classList.remove('open');document.body.style.overflow=''};closeBtn.addEventListener('click',closeModal);modal.addEventListener('click',(event)=>{if(event.target===modal){closeModal()}});document.addEventListener('keydown',(event)=>{if(event.key==='Escape'&&modal.classList.contains('open')){closeModal()}})})